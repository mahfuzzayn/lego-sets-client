import React from "react";
import useTitle from "../../../hooks/useTitle";

const Home = () => {
    useTitle("")
    return (
        <div className="home">        
        </div>
    );
};

export default Home;
